# DISABLED — upstream Open WebUI multi-arch Docker workflow
# Greg uses build.yml instead (single-arch, single-variant, GHCR push)
# Kept for reference if upstream sync needed
name: Create and publish Docker images with specific build args
on:
  workflow_dispatch:
    # Manual-only — never auto-triggers on push
