# DISABLED — upstream Ruff formatting checks
# Greg backend changes go through the CORTEX pipe function (edited in Open WebUI admin),
# not through upstream Python files. Re-enable when modifying upstream Python code.
name: Python CI
on:
  workflow_dispatch:
    # Manual-only — never auto-triggers on push
